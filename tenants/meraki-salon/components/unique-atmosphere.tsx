/**
 * Unique-lane slot (home only).
 *
 * Later: mount a restrained R3F atmosphere on void using DESIGN.md accent
 * only — no photography inside Canvas, dpr ≤ 1.5. Unmount when
 * prefers-reduced-motion is reduce or the document is hidden.
 *
 * Minimal lane: do not import three / @react-three/fiber / Canvas.
 */
export function UniqueAtmosphere() {
  return null
}
